#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../partial.js/bin/partial" "$@"
  ret=$?
else 
  node  "$basedir/../partial.js/bin/partial" "$@"
  ret=$?
fi
exit $ret
